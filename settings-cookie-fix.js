function bootCookieSettings(){const form=document.getElementById("profileForm");if(!form||form.dataset.cookieFixInstalled)return;form.dataset.cookieFixInstalled="true";document.addEventListener("submit",async e=>{if(e.target!==form)return;let r=null;try{r=await fetch("/api/me",{credentials:"same-origin",cache:"no-store"})}catch{return}const d=await r.json().catch(()=>({}));if(!r.ok||!d.ok||!d.user?.discordId)return;e.preventDefault();e.stopImmediatePropagation();const name=String(document.getElementById("displayName")?.value||"").trim();if(!name)return;const save=await fetch("/api/update-profile",{method:"POST",credentials:"same-origin",headers:{"Content-Type":"application/json"},body:JSON.stringify({name})});const out=await save.json().catch(()=>({}));const box=document.getElementById("statusMessage");if(box){box.textContent=save.ok?"Numele a fost salvat cu succes!":(out.error||"Nu s-a putut salva numele.");box.className=`alert-box ${save.ok?"alert-success":"alert-error"}`;box.style.display="block";setTimeout(()=>box.style.display="none",5000)}},true)}
if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",bootCookieSettings,{once:true});else bootCookieSettings();