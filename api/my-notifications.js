const crypto = require("crypto");
const PROJECT_ID = "pcls-portal";
const SESSION_COOKIE = "pcls_discord_session";
const MAX_AGE = 60 * 60 * 24 * 7;
function parseCookie(req,name){const raw=String(req.headers.cookie||"");for(const part of raw.split(";")){const [key,...rest]=part.trim().split("=");if(key===name)return decodeURIComponent(rest.join("="))}return ""}
function readSession(req){const raw=parseCookie(req,SESSION_COOKIE);const secret=String(process.env.DISCORD_CLIENT_SECRET||"").trim();if(!raw||!secret)return null;const [body,signature]=raw.split(".");if(!body||!signature)return null;const expected=crypto.createHmac("sha256",secret).update(body).digest("base64url");if(signature.length!==expected.length||!crypto.timingSafeEqual(Buffer.from(signature),Buffer.from(expected)))return null;try{const payload=JSON.parse(Buffer.from(body,"base64url").toString("utf8"));const now=Math.floor(Date.now()/1000);if(!payload?.discordId||!payload?.exp||payload.exp<=now||payload.exp>now+MAX_AGE+60)return null;return payload}catch{return null}}
function fv(v){if(typeof v==="boolean")return {booleanValue:v};if(typeof v==="number"&&Number.isFinite(v))return Number.isInteger(v)?{integerValue:String(v)}:{doubleValue:v};return {stringValue:String(v??"")}}
function decode(fields={}){const out={};for(const[k,v]of Object.entries(fields)){if(v.stringValue!==undefined)out[k]=v.stringValue;else if(v.integerValue!==undefined)out[k]=Number(v.integerValue);else if(v.doubleValue!==undefined)out[k]=v.doubleValue;else if(v.booleanValue!==undefined)out[k]=v.booleanValue;else if(v.timestampValue!==undefined)out[k]=v.timestampValue;else if(v.nullValue!==undefined)out[k]=null;else if(v.mapValue)out[k]=decode(v.mapValue.fields||{});else if(v.arrayValue)out[k]=(v.arrayValue.values||[]).map(x=>decode({x}).x)}return out}
function encodeFields(obj={}){const fields={};for(const[k,v]of Object.entries(obj))fields[k]=fv(v);return fields}
function b64(v){return Buffer.from(v).toString("base64url")}
function signJwt(h,p,key){const input=`${b64(JSON.stringify(h))}.${b64(JSON.stringify(p))}`;const sig=crypto.createSign("RSA-SHA256").update(input).end().sign(key);return `${input}.${sig.toString("base64url")}`}
async function accessToken(){const raw=String(process.env.FIREBASE_SERVICE_ACCOUNT_JSON||"").trim();if(!raw)throw new Error("FIREBASE_SERVICE_ACCOUNT_JSON lipsește.");const sa=JSON.parse(raw);const now=Math.floor(Date.now()/1000);const assertion=signJwt({alg:"RS256",typ:"JWT"},{iss:sa.client_email,sub:sa.client_email,aud:"https://oauth2.googleapis.com/token",iat:now,exp:now+3600,scope:"https://www.googleapis.com/auth/datastore"},sa.private_key.replace(/\\n/g,"\n"));const r=await fetch("https://oauth2.googleapis.com/token",{method:"POST",headers:{"Content-Type":"application/x-www-form-urlencoded"},body:new URLSearchParams({grant_type:"urn:ietf:params:oauth:grant-type:jwt-bearer",assertion}).toString()});const d=await r.json().catch(()=>({}));if(!r.ok||!d.access_token)throw new Error("Nu s-a putut obține tokenul Google.");return d.access_token}
async function query(token,field,value){const r=await fetch(`https://firestore.googleapis.com/v1/projects/${PROJECT_ID}/databases/(default)/documents:runQuery`,{method:"POST",headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json"},body:JSON.stringify({structuredQuery:{from:[{collectionId:"notificari"}],where:{fieldFilter:{field:{fieldPath:field},op:"EQUAL",value:{stringValue:String(value)}}},limit:100}})});if(!r.ok)return[];const rows=await r.json().catch(()=>[]);return Array.isArray(rows)?rows.filter(x=>x.document).map(x=>({id:String(x.document.name).split("/").pop(),...decode(x.document.fields||{})})):[]}
async function patch(token,id,data){const url=new URL(`https://firestore.googleapis.com/v1/projects/${PROJECT_ID}/databases/(default)/documents/notificari/${encodeURIComponent(id)}`);for(const key of Object.keys(data))url.searchParams.append("updateMask.fieldPaths",key);const r=await fetch(url,{method:"PATCH",headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json"},body:JSON.stringify({fields:encodeFields(data)})});if(!r.ok)throw new Error("Nu s-a putut actualiza notificarea.")}
module.exports=async function(req,res){res.setHeader("Cache-Control","no-store");const session=readSession(req);if(!session)return res.status(401).json({ok:false,error:"Nu ești autentificat cu Discord."});try{const token=await accessToken();if(req.method==="GET"){const [byDiscord,byId]=await Promise.all([query(token,"recipientDiscordId",session.discordId),query(token,"recipientId",session.discordId)]);const map=new Map();[...byDiscord,...byId].forEach(x=>map.set(x.id,x));const notifications=[...map.values()].sort((a,b)=>Date.parse(String(b.createdAt||""))-Date.parse(String(a.createdAt||""));return res.status(200).json({ok:true,notifications,user:{discordId:String(session.discordId),username:String(session.username||session.name||"")}})}if(req.method!=="POST")return res.status(405).json({ok:false,error:"Method Not Allowed"});const body=typeof req.body==="object"&&req.body?req.body:{};if(body.action==="read"){await patch(token,String(body.id||""),{read:true});return res.status(200).json({ok:true})}if(body.action==="readAll"){const [a,b]=await Promise.all([query(token,"recipientDiscordId",session.discordId),query(token,"recipientId",session.discordId)]);const map=new Map();[...a,...b].forEach(x=>map.set(x.id,x));await Promise.all([...map.keys()].map(id=>patch(token,id,{read:true})));return res.status(200).json({ok:true})}return res.status(400).json({ok:false,error:"Acțiune necunoscută."})}catch(e){console.error("my-notifications",e);return res.status(500).json({ok:false,error:e.message||"Eroare server."})}};