const crypto=require("crypto");const PROJECT_ID="pcls-portal",SESSION_COOKIE="pcls_discord_session",MAX_AGE=60*60*24*7,CACHE_TTL=15000;const ROLE_PERMISSIONS={superadmin:["all"],admin:["all"],conducere:["isuls","dsls","mmls","ssmls"],isuls:["isuls"],dsls:["dsls"],mmls:["mmls","mm"],ssmls:["ssmls","ssmmls"]};const VALID_ROLES=Object.keys(ROLE_PERMISSIONS);let cachedFirebaseToken=null,cachedFirebaseTokenExp=0,cachedServiceAccount=null,cachedProfiles=new Map,cachedAllRequests=null,cachedAllRequestsAt=0;function json(res,status,body){return res.status(status).json(body)}function parseCookie(req,name){const raw=String(req.headers.cookie||"");for(const part of raw.split(";")){const[key,...rest]=part.trim().split("=");if(key===name)return decodeURIComponent(rest.join("="))}return""}function readSession(req){const raw=parseCookie(req,SESSION_COOKIE),secret=String(process.env.DISCORD_CLIENT_SECRET||"").trim();if(!raw||!secret)return null;const[body,signature]=raw.split(".");if(!body||!signature)return null;const expected=crypto.createHmac("sha256",secret).update(body).digest("base64url");if(signature.length!==expected.length||!crypto.timingSafeEqual(Buffer.from(signature),Buffer.from(expected)))return null;try{const payload=JSON.parse(Buffer.from(body,"base64url").toString("utf8")),now=Math.floor(Date.now()/1000);if(!payload?.discordId||!payload?.exp||payload.exp<=now||payload.exp>now+MAX_AGE+60)return null;return payload}catch{return null}}function fv(v){if(typeof v==="boolean")return{booleanValue:v};if(typeof v==="number"&&Number.isFinite(v))return Number.isInteger(v)?{integerValue:String(v)}:{doubleValue:v};return{stringValue:String(v??"")}}function decode(fields={}){const out={};for(const[k,v]of Object.entries(fields)){if(v.stringValue!==undefined)out[k]=v.stringValue;else if(v.integerValue!==undefined)out[k]=Number(v.integerValue);else if(v.doubleValue!==undefined)out[k]=v.doubleValue;else if(v.booleanValue!==undefined)out[k]=v.booleanValue;else if(v.timestampValue!==undefined)out[k]=v.timestampValue;else if(v.nullValue!==undefined)out[k]=null;else if(v.mapValue)out[k]=decode(v.mapValue.fields||{});else if(v.arrayValue)out[k]=(v.arrayValue.values||[]).map(x=>decode({x}).x)}return out}function encodeFields(obj={}){const fields={};for(const[k,v]of Object.entries(obj))fields[k]=fv(v);return fields}function b64(v){return Buffer.from(v).toString("base64url")}function signJwt(h,p,key){const input=`${b64(JSON.stringify(h))}.${b64(JSON.stringify(p))}`,sig=crypto.createSign("RSA-SHA256").update(input).end().sign(key);return`${input}.${sig.toString("base64url")}`}async function serviceAccount(){if(cachedServiceAccount)return cachedServiceAccount;const raw=String(process.env.FIREBASE_SERVICE_ACCOUNT_JSON||"").trim();if(!raw)throw new Error("FIREBASE_SERVICE_ACCOUNT_JSON lipsește.");const sa=JSON.parse(raw);if(!sa.client_email||!sa.private_key)throw new Error("FIREBASE_SERVICE_ACCOUNT_JSON invalid.");cachedServiceAccount=sa;return sa}async function accessToken(sa){const now=Math.floor(Date.now()/1000);if(cachedFirebaseToken&&cachedFirebaseTokenExp>now+60)return cachedFirebaseToken;const assertion=signJwt({alg:"RS256",typ:"JWT"},{iss:sa.client_email,sub:sa.client_email,aud:"https://oauth2.googleapis.com/token",iat:now,exp:now+3600,scope:"https://www.googleapis.com/auth/datastore"},sa.private_key.replace(/\\n/g,"\n"));const r=await fetch("https://oauth2.googleapis.com/token",{method:"POST",headers:{"Content-Type":"application/x-www-form-urlencoded"},body:new URLSearchParams({grant_type:"urn:ietf:params:oauth:grant-type:jwt-bearer",assertion}).toString()}),d=await r.json().catch(()=>({}));if(!r.ok||!d.access_token)throw new Error(`Google token indisponibil (${r.status}).`);cachedFirebaseToken=d.access_token;cachedFirebaseTokenExp=now+3500;return cachedFirebaseToken}async function runQuery(token,collectionId,fieldPath,value,limit=5){const r=await fetch(`https://firestore.googleapis.com/v1/projects/${PROJECT_ID}/databases/(default)/documents:runQuery`,{method:"POST",headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json"},body:JSON.stringify({structuredQuery:{from:[{collectionId}],where:{fieldFilter:{field:{fieldPath},op:"EQUAL",value:fv(value)}},limit}})});if(!r.ok)return[];const rows=await r.json().catch(()=>[]);return Array.isArray(rows)?rows.filter(x=>x.document).map(x=>({id:String(x.document.name).split("/").pop(),...decode(x.document.fields||{})})):[]}async function listCollection(token,collectionId){const out=[];let pageToken="";do{const url=new URL(`https://firestore.googleapis.com/v1/projects/${PROJECT_ID}/databases/(default)/documents/${collectionId}`);url.searchParams.set("pageSize","1000");if(pageToken)url.searchParams.set("pageToken",pageToken);const r=await fetch(url,{headers:{Authorization:`Bearer ${token}`}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(`Firestore ${collectionId} ${r.status}: ${d?.error?.message||"request failed"}`);for(const document of d.documents||[])out.push({id:String(document.name).split("/").pop(),...decode(document.fields||{})});pageToken=d.nextPageToken||""}while(pageToken);return out}async function getDocument(token,collectionId,id){const r=await fetch(`https://firestore.googleapis.com/v1/projects/${PROJECT_ID}/databases/(default)/documents/${collectionId}/${encodeURIComponent(id)}`,{headers:{Authorization:`Bearer ${token}`}});if(r.status===404)return null;if(!r.ok){const d=await r.json().catch(()=>({}));throw new Error(`Firestore read ${r.status}: ${d?.error?.message||"request failed"}`)}const d=await r.json();return{id:String(d.name).split("/").pop(),...decode(d.fields||{})}}async function patchDoc(token,collectionId,id,data){const url=new URL(`https://firestore.googleapis.com/v1/projects/${PROJECT_ID}/databases/(default)/documents/${collectionId}/${encodeURIComponent(id)}`);for(const key of Object.keys(data))url.searchParams.append("updateMask.fieldPaths",key);const r=await fetch(url,{method:"PATCH",headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json"},body:JSON.stringify({fields:encodeFields(data)})});if(!r.ok){const d=await r.json().catch(()=>({}));throw new Error(`Firestore update ${r.status}: ${d?.error?.message||"request failed"}`)}}async function deleteDoc(token,collectionId,id){const r=await fetch(`https://firestore.googleapis.com/v1/projects/${PROJECT_ID}/databases/(default)/documents/${collectionId}/${encodeURIComponent(id)}`,{method:"DELETE",headers:{Authorization:`Bearer ${token}`}});if(!r.ok)throw new Error(`Firestore delete ${r.status}.`)}async function createDoc(token,collectionId,data){const r=await fetch(`https://firestore.googleapis.com/v1/projects/${PROJECT_ID}/databases/${encodeURIComponent("(default)")}/documents/${collectionId}`,{method:"POST",headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json"},body:JSON.stringify({fields:encodeFields(data)})});if(!r.ok)throw new Error("Nu s-a putut crea documentul.");return r.json()}function firstValue(item,keys){for(const key of keys){const value=String(item?.[key]??"").trim();if(value)return value}return""}async function findProfile(token,session){const discordId=String(session?.discordId||"").trim(),email=String(session?.email||"").trim(),cacheKey=discordId||email;if(cacheKey){const hit=cachedProfiles.get(cacheKey);if(hit&&hit.expiresAt>Date.now())return hit.profile}let profile=null;if(discordId){const rows=await runQuery(token,"utilizatori","discordId",discordId,1);if(rows[0])profile=rows[0]}if(!profile&&email){const rows=await runQuery(token,"utilizatori","email",email,1);if(rows[0])profile=rows[0]}if(cacheKey)cachedProfiles.set(cacheKey,{profile,expiresAt:Date.now()+60000});return profile}function normalizeRole(v){const r=String(v||"").trim().toLowerCase().replace(/\s+/g,"");if(r==="conducerea")return"conducere";if(r==="mm"||r==="mmlls")return"mmls";if(r==="ssmmls")return"ssmls";return r}async function authorize(token,session){const sessionRole=normalizeRole(session.role||"");if(VALID_ROLES.includes(sessionRole))return{ok:true,role:sessionRole,profile:null};const profile=await findProfile(token,session),role=normalizeRole(profile?.role||profile?.rol||"");return{ok:VALID_ROLES.includes(role),role,profile}}async function notifyOwner(token,item,status,actor,reason){let recipientId=firstValue(item,["ownerUid","ownerUID","firebaseUid","firebaseUID","userUid","userUID","uid","userId","user_id","utilizatorId","createdByUid","created_by_uid","submittedByUid","requesterUid","requesterId"]);let discordId=firstValue(item,["discordId","discord_id","discord"]);if(!recipientId||!discordId){const ownerProfile=await findProfile(token,{discordId,email:firstValue(item,["email","emailAddress"])});if(!recipientId)recipientId=ownerProfile?.id||"";if(!discordId)discordId=firstValue(ownerProfile,["discordId","discord_id","discord"])}if(!recipientId)return;const normalized=String(status||"").toLowerCase(),isApproved=["aprobat","acceptat","approved"].includes(normalized),isRejected=["respins","respinsa","rejected"].includes(normalized);if(!isApproved&&!isRejected)return;const cleanReason=String(reason||"").trim();await createDoc(token,"notificari",{recipientId:String(recipientId),recipientDiscordId:discordId||null,title:isApproved?"Cerere aprobată":"Cerere respinsă",message:isApproved?`Cererea ta a fost acceptată de ${actor}.`:`Cererea ta a fost respinsă de ${actor}.${cleanReason?` Motiv: ${cleanReason}`:""}`,type:isApproved?"success":"error",requestId:String(item.id||""),requestUrl:item.id?`cererile_mele.html?cerere=${encodeURIComponent(item.id)}`:null,status:normalized,actorName:actor,actorRole:"administrator",rejectionReason:isRejected?(cleanReason||null):null,read:false,createdAt:new Date().toISOString()})}function canSee(role,item){if(role==="superadmin"||role==="admin")return true;const text=[item.departament,item.departament_medical,item.department,item.dept,item.tip_cerere,item.tip,item.eveniment].filter(Boolean).join(" ").toLowerCase();return(ROLE_PERMISSIONS[role]||[]).some(k=>k!=="all"&&text.includes(k))}module.exports=async function(req,res){res.setHeader("Cache-Control","no-store");const session=readSession(req);if(!session)return json(res,401,{ok:false,error:"Nu ești autentificat cu Discord."});const fallbackActor=String(session.globalName||session.username||session.name||session.email||session.discordId||"Admin PCLS").replace(/^@/,"");try{const sa=await serviceAccount(),token=await accessToken(sa),auth=await authorize(token,session),actorProfile=auth.profile;const profileActor=firstValue(actorProfile,["nume","name","displayName","numeAfisat","nume_afisat","username"]);const actor=String(profileActor||fallbackActor).replace(/^@/,""),actorLabel=`@${actor}`;if(!auth.ok)return json(res,403,{ok:false,error:"Nu ai un rol de administrator.",role:auth.role||null});if(req.method==="GET"){const now=Date.now();if(cachedAllRequests&&now-cachedAllRequestsAt<CACHE_TTL){const requests=cachedAllRequests.filter(x=>canSee(auth.role,x));return json(res,200,{ok:true,user:{discordId:session.discordId,name:actor,username:session.username||actor,email:session.email||"",role:auth.role},role:auth.role,requests,fromCache:true})}const all=await listCollection(token,"cereri");cachedAllRequests=all;cachedAllRequestsAt=now;const requests=all.filter(x=>canSee(auth.role,x)).sort((a,b)=>Date.parse(String(b.created_at||b.data_creare||b.createdAt||b.data||""))-Date.parse(String(a.created_at||a.data_creare||a.createdAt||a.data||"")));return json(res,200,{ok:true,user:{discordId:session.discordId,name:actor,username:session.username||actor,email:session.email||"",role:auth.role},role:auth.role,requests})}if(req.method!=="POST")return json(res,405,{ok:false,error:"Method Not Allowed"});const body=typeof req.body==="object"&&req.body?req.body:{},action=String(body.action||"");if(action==="generateInvite"){if(!["superadmin","admin","conducere"].includes(auth.role))return json(res,403,{ok:false,error:"Nu ai permisiunea."});const selected=String(body.role||"isuls").toLowerCase();if(!["isuls","dsls","mmls","ssmls"].includes(selected))return json(res,400,{ok:false,error:"Rol invalid."});const code=`${selected.toUpperCase()}-${Math.random().toString(36).slice(2,8).toUpperCase()}`;await createDoc(token,"invite_codes",{code,role:selected,used:false,created_at:new Date().toISOString(),created_by:actorLabel});return json(res,200,{ok:true,code})}const id=String(body.id||"");if(!id)return json(res,400,{ok:false,error:"ID lipsă."});const item=await getDocument(token,"cereri",id);if(!item||!canSee(auth.role,item))return json(res,404,{ok:false,error:"Cererea nu există sau nu ai acces."});const nowLocal=new Date().toLocaleString("ro-RO");if(action==="status"){const st=String(body.status||""),reason=firstValue(body,["reason","motiv","motivRespingere","rejectionReason"]),processedLabel=st==="aprobat"?`Aprobat de: ${actorLabel}`:st==="respins"?`Respins de: ${actorLabel}`:`Procesat de: ${actorLabel}`,updates={status:st,procesat_de:processedLabel,procesat_de_nume:actorLabel,data_procesare:nowLocal,deleted:st==="in_cos"};if(st==="respins"){updates.motiv_respingere=reason||firstValue(item,["motiv_respingere","motivRespingere"])||"";updates.motivRespingere=updates.motiv_respingere}await patchDoc(token,"cereri",id,updates);cachedRequests=null;cachedRequestsAt=0;cachedAllRequests=null;cachedAllRequestsAt=0;if(["aprobat","respins"].includes(st)){try{await notifyOwner(token,{...item,...updates,id},st,actorLabel,updates.motiv_respingere||reason)}catch(e){console.error("Notification status error:",e)}}return json(res,200,{ok:true,actor:actorLabel,processedLabel:st==="aprobat"?`A fost acceptată de: ${actorLabel}`:st==="respins"?`A fost respinsă de: ${actorLabel}`:`Procesată de: ${actorLabel}`})}if(action==="archive"){const yes=Boolean(body.value);await patchDoc(token,"cereri",id,{arhivat:yes,archived:yes,procesat_de:`${actorLabel} (${yes?"Arhivat":"Desarhivat"})`,procesat_de_nume:actorLabel,data_procesare:nowLocal});cachedAllRequests=null;cachedAllRequestsAt=0;return json(res,200,{ok:true,actor:actorLabel})}if(action==="delete"){await deleteDoc(token,"cereri",id);cachedRequests=null;cachedRequestsAt=0;cachedAllRequests=null;cachedAllRequestsAt=0;return json(res,200,{ok:true})}return json(res,400,{ok:false,error:"Acțiune necunoscută."})}catch(e){console.error("admin-data error",e);return json(res,500,{ok:false,error:e?.message||"Eroare server."})}};